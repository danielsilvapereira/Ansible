---
- name: Deploy do Zabbix Proxy
  hosts: "zabbix_proxy"
  become: true
  vars:
    - zabbix_release_version: 6.0
  
  roles:
    - zabbixproxy

  post_tasks:
    - include_role:
        name: zabbixproxy
        tasks_from: setup.yml