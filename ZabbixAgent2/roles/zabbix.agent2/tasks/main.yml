- name: Iniciando deploy do Zabbix-Agent2 - {{ansible_os_family}} - {{ansible_distribution}}-{{ansible_distribution_major_version}}
  include_tasks: deploy_rhel.yml
  when:  ansible_os_family == "RedHat"

- name: Iniciando deploy do Zabbix-Agent2 - {ansible_os_family}} - {{ansible_distribution}}-{{ansible_distribution_major_version}}
  include_tasks: deploy_ubuntu_debian.yml
  when:  ansible_os_family == "Debian" or ansible_os_family == "Ubuntu"